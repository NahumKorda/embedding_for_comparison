This package generates train, val and test datasets from the original sentences (considered to be the "positive
samples") and two independent datasets consisting of individual sentences of various sizes (used as the candidates
for the "negative "samples").
