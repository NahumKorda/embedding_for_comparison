This package is used to train a text embedding model.

Run the training_data_generator.py script to combine positive and negative samples into triples consisting of an
anchor (the original sentence), positive (synthetic rephrase of the anchor) and negative (selected negative sample).

Run trainer.py script to train or fine-tune the text embedding model. Training session is configured using
config.yaml file.
